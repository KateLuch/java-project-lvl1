name: Kate GitHub Actions
on:
  push:
    branches:
      - '**'
    tags:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          java-version: '16'
      - run: |
          cd ${{ github.workspace }}
          make build